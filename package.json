{
  "name": "my-monorepo-root",
  "private": true,
  "scripts": {
    "start": "pm2 start",
    "dev": "pm2 start",
    "dev:frontend": "pm2 start --only frontend",
    "dev:backend": "pm2 start --only backend",
    "stop": "pm2 stop all",
    "stop:frontend": "pm2 stop frontend",
    "stop:backend": "pm2 stop backend",
    "status": "pm2 list",
    "logs": "pm2 logs"
  },
  "devDependencies": {
    "pm2": "^6.0.14"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "devEngines": {
    "packageManager": { "name": "pnpm", "version": ">=11.0.0", "onFail": "download" }
  }
}